<p id="notice"><%= notice %></p>

<h1>EHRs</h1>

<table>
  <thead>
    <tr>
      <th>EHR ID</th>
      <th>No Compositions</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ehrs.each do |ehr| %>
      <tr>
        <td><%= ehr.id %></td>
        <td><%= ehr.person.name %></td>
        <td><%= link_to 'Show', ehr_monshins_path(ehr.id) %></td>
        <td><%= link_to 'Edit', edit_ehr_path(ehr.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br />
